---
netbox_mac_addresses_mnbox01:
  - assigned_object:
      name: "kube0"
      device: "mnbox01"
    mac_address: "{{ mac_mnbox01_kube0 }}"
  - assigned_object:
      name: "ceph0"
      device: "mnbox01"
    mac_address: "{{ mac_mnbox01_ceph0 }}"

