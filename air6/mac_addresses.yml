---
netbox_mac_addresses_air6:
  - assigned_object:
      name: "wlan0"
      device: "air6"
    mac_address: "{{ mac_air6_wlan0 | upper }}"

  - assigned_object:
      name: "eth0"
      device: "air6"
    mac_address: "{{ mac_air6_eth0 | upper }}"

