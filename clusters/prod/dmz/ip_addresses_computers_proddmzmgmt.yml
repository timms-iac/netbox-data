---
netbox_ip_addresses_computers_proddmzmgmt:

  - address: "172.25.40.250/32"
    assigned_object:
      device: "kiab"
      name: "proddmzmgmt0"
    vrf: "proddmzmgmt"

  - address: "172.25.40.253/32"
    role: "VRRP"
    vrf: "proddmzmgmt"
    assigned_object_id: '{{ query("netbox.netbox.nb_lookup", "fhrp-groups",
        api_filter="group_id=2540",
        api_endpoint="http://" + netbox_loadbalancer_ip,
        token=netbox_ansible_token) | map(attribute="key") | join(",") }}'
    assigned_object_type: "ipam.fhrpgroup"

