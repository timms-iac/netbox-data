---
netbox_ip_addresses_computers_prodosmgmt:

  - address: "172.25.30.250/32"
    assigned_object:
      device: "kiab"
      name: "prodosmgmt0"
    vrf: "prodosmgmt"

  - address: "172.25.30.253/32"
    role: "VRRP"
    vrf: "prodosmgmt"
    assigned_object_id: '{{ query("netbox.netbox.nb_lookup", "fhrp-groups",
        api_filter="group_id=2530",
        api_endpoint="http://" + netbox_loadbalancer_ip,
        token=netbox_ansible_token) | map(attribute="key") | join(",") }}'
    assigned_object_type: "ipam.fhrpgroup"

