---
netbox_ip_addresses_computers_devk8smgmt:

  - address: "172.20.20.250/32"
    assigned_object:
      device: "kiab"
      name: "devk8smgmt0"
    vrf: "devk8smgmt"

  - address: "172.20.20.253/32"
    role: "VRRP"
    vrf: "devk8smgmt"
    assigned_object_id: '{{ query("netbox.netbox.nb_lookup", "fhrp-groups",
        api_filter="group_id=2020",
        api_endpoint="http://" + netbox_loadbalancer_ip,
        token=netbox_ansible_token) | map(attribute="key") | join(",") }}'
    assigned_object_type: "ipam.fhrpgroup"

