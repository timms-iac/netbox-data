---
netbox_ip_addresses_computers_devosmgmt:

  - address: "172.20.30.250/32"
    assigned_object:
      device: "kiab"
      name: "devosmgmt0"
    vrf: "devosmgmt"

  - address: "172.20.30.253/32"
    role: "VRRP"
    vrf: "devosmgmt"
    assigned_object_id: '{{ query("netbox.netbox.nb_lookup", "fhrp-groups",
        api_filter="group_id=2030",
        api_endpoint="http://" + netbox_loadbalancer_ip,
        token=netbox_ansible_token) | map(attribute="key") | join(",") }}'
    assigned_object_type: "ipam.fhrpgroup"

