---
netbox_mac_addresses_try03:
  - assigned_object:
      name: "mgmt0"
      device: "try03"
    mac_address: "{{ mac_try03_mgmt0 }}"
  - assigned_object:
      name: "kube0"
      device: "try03"
    mac_address: "{{ mac_try03_kube0 }}"
  - assigned_object:
      name: "ceph0"
      device: "try03"
    mac_address: "{{ mac_try03_ceph0 }}"

