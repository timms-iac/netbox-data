---
netbox_wireless_lans_BER1:
  - ssid: "{{ ssid_android }}"
    auth_psk: "{{ psk_android }}"
    vlan:
      name: "Android"
  - ssid: "{{ ssid_iot }}"
    auth_psk: "{{ psk_iot }}"
    vlan:
      name: "iot"
  - ssid: "{{ ssid_shelly }}"
    auth_psk: "{{ psk_shelly }}"
    vlan:
      name: "Shelly"
  - ssid: "{{ ssid_work }}"
    auth_psk: "{{ psk_work }}"
    vlan:
      name: "Work"
  - ssid: "{{ ssid_dmz }}"
    auth_psk: "{{ psk_dmz }}"
    vlan:
      name: "dmz"
  - ssid: "{{ ssid_guest }}"
    auth_psk: "{{ psk_guest }}"
    vlan:
      name: "guest"
